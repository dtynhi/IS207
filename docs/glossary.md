# Glossary

- Parity-first: migrate behavior before introducing new features.
- Vertical slice: migrate one end-to-end feature across backend + frontend + data.
- Strangler migration: gradually replace legacy parts while system stays live.
