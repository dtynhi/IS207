# Frontend Developer Guide

## Screen flow

`route -> page -> feature components -> query hooks -> API client`

## Rules

- Use TanStack Query for API calls.
- Keep components presentational when possible.
- Co-locate feature UI and hooks under `src/features/<feature>`.
